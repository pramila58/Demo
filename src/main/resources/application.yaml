spring:
  datasource:
    url: jdbc:db2://localhost:50000/employee
    username: db2admin
    password: 2000
    driver-class-name: com.ibm.db2.jcc.DB2Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.DB2Dialect
logging:
  level:
    org.springframework: DEBUG

